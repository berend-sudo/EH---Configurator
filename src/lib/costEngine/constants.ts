/** Margin applied on cost ex VAT (Excel B39). */
export const MARGIN = 0.10;

/** VAT applied on sales price ex VAT (Excel B40). */
export const VAT = 0.18;

/** UGX per USD conversion rate (Excel B38). */
export const USD_TO_UGX = 3700;

/** Display rounding granularity for client-facing UGX prices (round UP). */
export const PRICE_DISPLAY_ROUND_UGX = 100_000;

/** Frame and module dimensions (mm). Per spec — used by Phase 2 length slider. */
export const FRAME_A_MM = 1221;
export const FRAME_B_MM = 2442;
export const FRAME_C_MM = 3053;
export const JUMP_MM = 610;
export const MIN_JUMPS = 8;
export const MAX_JUMPS = 20;
